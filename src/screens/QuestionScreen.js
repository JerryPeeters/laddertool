import React from 'react';
import { Question } from '../components';

export default class extends React.Component {
    render() {
        return (
            <Question />
        )
    }
}